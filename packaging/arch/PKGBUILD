pkgname=git-sync-bin
pkgver=${PKGVER:-0.0.0}
pkgrel=${PKGREL:-1}
pkgdesc="Air-gap Git sync and audit tool (prebuilt binary package)"
arch=('x86_64')
url="https://github.com/daredem0/git-sync"
license=('Apache')
depends=('glibc')
provides=('git-sync')
conflicts=('git-sync')
source=(
  "git-sync"
  "git-sync.1.gz"
  "git-sync-readme.7.gz"
  "git-sync-architecture.7.gz"
  "LICENSE"
)
sha256sums=('SKIP' 'SKIP' 'SKIP' 'SKIP' 'SKIP')

build() {
  :
}

package() {
  install -Dm755 "${srcdir}/git-sync" "${pkgdir}/usr/bin/git-sync"
  install -Dm644 "${srcdir}/git-sync.1.gz" \
    "${pkgdir}/usr/share/man/man1/git-sync.1.gz"
  install -Dm644 "${srcdir}/git-sync-readme.7.gz" \
    "${pkgdir}/usr/share/man/man7/git-sync-readme.7.gz"
  install -Dm644 "${srcdir}/git-sync-architecture.7.gz" \
    "${pkgdir}/usr/share/man/man7/git-sync-architecture.7.gz"
  install -Dm644 "${srcdir}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
